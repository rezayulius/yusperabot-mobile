import 'package:flutter/material.dart';
// Import package atau library yang diperlukan untuk menampilkan AR

class ARScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // Kodingan untuk menampilkan AR, sesuaikan dengan kebutuhan Anda
    return Scaffold(
      appBar: AppBar(
        title: Text('Hasil Desain 3D'),
      ),
      body: Center(
        child: Text('Tampilan AR akan ditampilkan di sini'),
      ),
    );
  }
}
